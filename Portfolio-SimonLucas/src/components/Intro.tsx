// components/Intro.tsx

interface Props {
  onContactOpen: () => void;
}

export default function Intro({ onContactOpen }: Props) {
  return (
    <section>
      {/* ta section intro ici */}
    </section>
  );
}